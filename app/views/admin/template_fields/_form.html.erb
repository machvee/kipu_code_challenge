<%= form_with(model: template_field, local: true) do |form| %>
  <% if template_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(template_field.errors.count, "error") %> prohibited this template_field from being saved:</h2>

      <ul>
      <% template_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :field_name %>
    <%= form.text_field :field_name %>
  </div>

  <div class="field">
    <%= form.label :placeholder %>
    <%= form.text_area :placeholder %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
