<%= form_with(model: alergy, local: true) do |form| %>
  <% if alergy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alergy.errors.count, "error") %> prohibited this alergy from being saved:</h2>

      <ul>
      <% alergy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.text_field :patient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
