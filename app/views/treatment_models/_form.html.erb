<%= form_with(model: treatment_model, local: true) do |form| %>
  <% if treatment_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(treatment_model.errors.count, "error") %> prohibited this treatment_model from being saved:</h2>

      <ul>
      <% treatment_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :necessity %>
    <%= form.text_area :necessity %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.text_field :patient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
